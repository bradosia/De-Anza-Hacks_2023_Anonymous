version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip3 install
        - pip install flask
        - pip install python-dotenv
    build:
      commands:
        - python app.py
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
